<!DOCTYPE html>
<html lang="en">
<head>
    <meta charset="UTF-8">
    <meta name="viewport" content="width=device-width, initial-scale=1.0">
    <title>Quora Post</title>
    <link  rel="stylesheet" href="/index.css">
</head>
<body>
    <h1>This is Quora Post</h1>
    <% for(let post of posts) { %>
        <div class="post">
      <h3 class="user"><%= post.username %></h3>
      <h4 class="content"><%= post.content %></h4>

      <form method="Get" action="http://localhost:8080/posts/<%=post.id%>">
        <button>See in detail</button>
          </form>   
          <br>   
    <form method="Get" action="http://localhost:8080/posts/<%=post.id%>/edit">
    <button>Edit</button>
      </form>
      <form method="post" action="/posts/<%=post.id%>?_method=DELETE">
        <button>delete</button>
          </form>
    </div>
    <% } %>
    <br>
    <a href="http://localhost:8080/posts/new">Create new Quora Post</a>
</body>
</html>